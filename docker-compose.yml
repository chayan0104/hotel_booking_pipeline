services:
  oracle-db:
    image: gvenzl/oracle-xe:21-slim
    environment:
      - ORACLE_PASSWORD
      - APP_USER
      - APP_USER_PASSWORD
    ports:
      - "${ORACLE_PORT}:1521"
    volumes:
      - oracle_data:/opt/oracle/oradata

  rest-api:
    build:
      context: "./Rest Api"
    environment:
      - JAVA_OPTS
    ports:
      - "${API_PORT}:8080"
    depends_on:
      - oracle-db

  react-app:
    build:
      context: "./React App"
    ports:
      - "${WEB_PORT}:80"
    depends_on:
      - rest-api

volumes:
  oracle_data:
